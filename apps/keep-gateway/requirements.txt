# Dependencies for the Keep gateway service.
#
# The Keep side is pinned to exactly the versions the feasibility spike validated, so the
# deployed gateway talks to the private API the same way the spike proved it can:
#   gkeepapi  - the unofficial Keep client (the official API is Workspace-only)
#   gpsoauth  - the Android master-token flow it authenticates with
#   requests  - also used directly, to classify network vs. blocked failures
#
# Flask serves the HTTP boundary and gunicorn is the WSGI server Cloud Run starts.
# Python 3.10+ is required by gkeepapi; the image and the spike both use 3.14.
gkeepapi==0.17.1
gpsoauth==2.0.0
requests==2.34.2
Flask==3.1.3
gunicorn==26.2.0
