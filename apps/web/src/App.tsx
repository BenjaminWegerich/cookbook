/**
 * Root component of the web app.
 *
 * This is a neutral placeholder — the actual UI (smartphone + smart display
 * layout, custom typography) is designed in a later roadmap task together
 * with the user. The UI language is German (see docs/CODING_CONVENTIONS.md).
 */
function App() {
  return (
    <main className="app">
      <h1>Cookbook</h1>
      <p>Grundgerüst der Web-App — Design und Funktionen folgen.</p>
    </main>
  );
}

export default App;
