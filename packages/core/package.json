{
  "name": "@cookbook/core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Framework-free TypeScript module: deterministic scaling, additional-unit selection, recipe parsing (no React, no DOM).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "generate:ladder": "node scripts/generate-ladder.mjs",
    "generate:additional": "node scripts/generate-additional-data.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.13.3",
    "typescript": "^7.0.2",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
