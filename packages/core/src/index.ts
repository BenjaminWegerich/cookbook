/**
 * @cookbook/core — framework-free TypeScript module.
 *
 * Hosts all deterministic cookbook logic, independent of React and the DOM
 * (see docs/ARCHITECTURE.md):
 * - quantity scaling on the ladder of standard numbers (docs/quantity_scaling.md)
 * - additional-unit selection and display (docs/additional_quantity_specifications.md)
 * - recipe format parsing and validation (docs/storage_format.md)
 *
 * The implementations arrive with the "Core functionality" roadmap tasks.
 */

/** Version of the core module, kept in sync with packages/core/package.json. */
export const VERSION = '0.1.0';
